# estrategia de execução:  formatos de formularios do print  no testes. (variaveis)

default: -p pretty --publish-quiet -p html -p development -p chrome 

pretty: --format pretty 
development: ENVIRONMENT=development
production: ENVIRONMENT=production

chrome: BROWSER=chrome
firefox: BROWSER=firefox
edge: BROWSER=edge
chrome_hedless: BROWSER=chrome_headless
headless: BROWSER=headless

html: --format html --out=reports/report.html


# variaveis que recebe oque estao dentro delas